# This file is loaded inside nginx's `http` context from /etc/nginx/conf.d/.
# Keep this slightly larger than the application-level per-file limit because
# multipart requests also contain form fields and encoding overhead.
client_max_body_size 110M;
